{
  "name": "representante-vendas",
  "version": "0.0.18",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "deploy:web": "pnpm --filter @representante-vendas/web run deploy",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "@nestjs/core",
      "@scarf/scarf",
      "cpu-features",
      "esbuild",
      "protobufjs",
      "re2",
      "ssh2"
    ]
  }
}
