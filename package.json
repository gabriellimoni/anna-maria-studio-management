{
  "name": "anna-maria",
  "version": "0.0.37",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @anna-maria/web test:e2e",
    "test:e2e:ui": "pnpm --filter @anna-maria/web test:e2e:ui",
    "deploy:web": "pnpm --filter @anna-maria/web run deploy",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "@nestjs/core",
      "@scarf/scarf",
      "core-js",
      "cpu-features",
      "esbuild",
      "protobufjs",
      "re2",
      "ssh2"
    ]
  }
}
